import React from 'react';

const GuestPanel = () => (
  <div className='guest-panel'>
    <h2>🎫 Guest View</h2>
    <p>Read-only sanctified views and onboarding prophecy.</p>
  </div>
);

export default GuestPanel;
